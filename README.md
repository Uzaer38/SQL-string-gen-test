## Lightweight SQL builder

This is an F# library that provides strongly typed criteria to compose SQL queries using objects, and a parsing function that converts the query into an MS T-SQL string.

## To run

Open a terminal in the project repository and run:

```
dotnet run
```

This will execute the demo queries in Program.fs.

## Version

This program was developed in the Visual Studio 2026 environment and .NET.